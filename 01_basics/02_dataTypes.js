 'use strict' // treat all JS code as newer version
// alert( 3 + 3) // we are using nodejs, not browser. it doesn't support this so o/p will be undefined
// tc39 - ecma script262 tell us the standard of JS same with mdn

let name = "Simar"
let age = 24
let isLoggedIn = true
let state;

// number => 2 to power 53 (range)
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => 
// symbol => unique

// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object
